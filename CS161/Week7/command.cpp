/*
(5) Create one or more of the above programs to be command line arguments friendly (take in values from the command line instead of asking for them as user input)
(Note: You really do want to know why we would want to use command line arguments, so do a brief search
￼
and then discuss it on the discussion boards!)
*/
#include <iostream>
using namespace std;

int main()
{

    return 0;

}
