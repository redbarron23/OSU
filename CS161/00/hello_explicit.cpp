#include <iostream> // this is how we tell the compiler that we want a library in our code to use functions and variables from

// in this example file I am being explicit about what functionality I use (with the cout object)

int main() 
{
    std::cout << "hello, out there!?" << std::endl; //simple printout of "string literal" 
    
    return 0; // this tells the compiler that this is the end of this function, and to return
}
